{% extends 'DemoBundle::layout.html.twig' %}

{% block content %}
    <div class="row">
        <div class="col-md-6 col-md-offset-3">
            {{ form(form) }}
        </div>
    </div>
    <div class="row">
        <div class="col-md-6 col-md-offset-3">
            <a class="btn btn-danger" href="{{ path('demo_record_delete' , {'id' : record.id}) }}">Delete record</a>
        </div>
    </div>
    <div class="row">
        <div class="col-md-12">
            <img src="data:image/png;base64,{{ diagram }}" />
        </div>
    </div>
{% endblock %}